# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: ToDosProject
services:  
  app:
    language: dotnet
    project: ./src/ToDosProject.AppHost/ToDosProject.AppHost.csproj
    host: containerapp
